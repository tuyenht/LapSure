# C09 — Data Table

Dense structured technical information with readable headers/rows/status cells; sorting/filtering when useful. Must remain usable at target DPI, preserve critical columns, provide explicit empty/error state and avoid hiding required failures by default.
