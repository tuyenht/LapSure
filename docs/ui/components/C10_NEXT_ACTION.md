# C10 — Next Action Panel

Exactly one primary next-best action, reason and what remains. Bind to real workflow/orchestrator state. Never offer a bypass that converts mandatory missing evidence into clean completion.
