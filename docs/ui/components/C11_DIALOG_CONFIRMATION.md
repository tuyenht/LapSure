# C11 — Dialog & Confirmation

Modal for destructive/safety-sensitive choices. Show consequence and safe primary/secondary controls; destructive action is not default. Esc is safe. Never silently discard recorded evidence or interrupted-journal context.
