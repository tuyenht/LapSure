# S19 — Xuất báo cáo & Chia sẻ

`components: [C01,C02,C03,C04,C09,C10,C11,C12]`  
`visual: ../references/approved/S19_EXPORT_SHARE.jpg`

## User outcome
Export/share only formats and artifacts the build actually supports.

## Objects
HTML; JSON; print/PDF only if implemented; output path; package contents; privacy note.

## Data
Actual report/output capabilities and paths.

## Invariant
Do not display a successful PDF/share/cloud action if the capability does not exist. HTML/JSON evidence semantics must not regress.

## Acceptance
Unavailable format is disabled/explained; export result/path is explicit; no silent data loss.
