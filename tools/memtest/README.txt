LapSure includes a built-in ONLINE partial-coverage RAM test.
A clean result is intentionally WARNING/partial coverage, not a deep PASS.
Deep RAM certification should use a preboot tester such as MemTest86+ later.
